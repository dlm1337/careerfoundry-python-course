Recipe Model:
    Added pic as an imageField so there can be a recipe picture.
    Added get_absolute_url func to get the pk for recipe detail pages.
    Added calculate_difficulty function.

User Model:
    Added pic as an imageField so there can be a user picture.

Recipe Ingredient Model:
    Fixed a typo in amount_type selection.